// use an integer for version numbers
version = 1

android {
    buildFeatures {
        buildConfig = true
    }
}

cloudstream {
    language = "en"
    description = "AnimeSuge extension"
    authors = listOf("NivinCNC")
    requiresResources = false
}
